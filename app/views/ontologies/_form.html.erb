<%= form_for(@ontology) do |f| %>
  <% if @ontology.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ontology.errors.count, "error") %> prohibited this ontology from being saved:</h2>

      <ul>
      <% @ontology.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :file %><br>
    <div class="button" id="ontology-upload-button">Select File</div>
    <div id="file-name"></div>
    <%= f.file_field :file, id: "ontology-upload" %>
    <div class="float-break"></div>
  </div>



  <div class="field">
    <%= f.submit class: 'button' %>
  </div>
<% end %>
